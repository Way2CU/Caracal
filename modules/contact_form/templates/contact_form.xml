<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE document SYSTEM "../../../units/schema/template.dtd">
<document>
	<form class="contact" method="post">
		<input type="hidden" name="section" value="contact" class="hidden"/>
		<input type="hidden" name="action" value="send_message" class="hidden"/>
		<span>
			<_raw text="בדף זה תוכלו למלא את הפרטים הנדרשים כולל בקשה לתאום פגישה ואנו נשמח לעמוד בקשר."/><br/>
			<_raw text="דעו כי מתוקף החוק חל חיסיון מוחלט על כל הפרטים האישיים לרבות תוכן הפניה והם אינם מועברים לצד שלישי!"/><br/>
			<br/>
			<h5><_raw text="טלפון"/></h5>
			<_raw text="03-9220784"/><br/>
			<br/>
			<h5><_raw text="כתובת"/></h5>
			<_raw text="רחוב בילו 12 פתח תקווה"/><br/>
			<_raw text="info@kabalah.co.il"/><br/>
		</span>
		<table cellpadding="0" cellspacing="0" border="0" width="100%">
		<tr>
			<td valign="top">
				<_if condition="in_array('firstname', explode(',',$settings['fields']))">
					<_template file="field_firstname.xml" path="%module%templates/" />
				</_if>
				<_if condition="in_array('lastname', explode(',',$settings['fields']))">
					<_template file="field_lastname.xml" path="%module%templates/" />
				</_if>
				<_if condition="in_array('phone', explode(',',$settings['fields']))">
					<_template file="field_phone.xml" path="%module%templates/" />
				</_if>
				<_if condition="in_array('email', explode(',',$settings['fields']))">
					<_template file="field_email.xml" path="%module%templates/" />
				</_if>
				<_if condition="in_array('company', explode(',',$settings['fields']))">
					<_template file="field_company.xml" path="%module%templates/" />
				</_if>
			</td><td valign="top">
				<_if condition="in_array('message', explode(',',$settings['fields']))">
					<_template file="field_message.xml" path="%module%templates/" />
				</_if>
			</td>
		</tr></table>
		<div class="button_bar">
			<button type="reset"><_text constant="clear" module="contact_form"/></button>
			<button type="submit"><_text constant="send" module="contact_form"/></button>
		</div>
	</form><br/>
</document>
