<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE document SYSTEM "../../../units/schema/template.dtd">
<document>
	<label>
		<cms:if condition="$params['selected']">
			<input
				type="radio"
				name="delivery_method"
				value="$params['name']"
				eval="value"
				checked="checked"
			/>
		</cms:if>
		<cms:if condition="!$params['selected']">
			<input
				type="radio"
				name="delivery_method"
				value="$params['name']"
				eval="value"
			/>
		</cms:if>
		<span>
			<img
				src="$params['small_image']"
				alt="$params['title']"
				eval="src,alt"
			/>
		</span>
	</label>
</document>
